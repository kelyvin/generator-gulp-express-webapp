module.exports = function(server) {
    // Create Express API's here

    /*
    server.post('/', function (req, res) {
        var body = req.body;

        res.status(200).json({
            code: 200,
            message: "success"
        });
    });
    */
};